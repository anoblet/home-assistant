# Relies on packages/shared/presence/detection.yaml for the shared toggle.
living_room_presence_detection:
  google_assistant:
    entity_config:
      input_boolean.living_room_presence_detection:
        expose: true
        name: Living Room Presence Detection
