# Relies on packages/shared/presence/detection.yaml for the shared toggle.
kitchen_presence_detection:
  google_assistant:
    entity_config:
      input_boolean.kitchen_ld2410c:
        expose: true
        name: Kitchen Presence Detection
  input_boolean:
    kitchen_ld2410c:
      name: Kitchen Presence Detection
      initial: on
      icon: mdi:motion-sensor
