# Legacy LuCI device tracker configuration noted for documentation.
# The integration requires the luci-mod-rpc package on the OpenWrt router
# at 192.168.1.1. Install the dependency and restore the block below in
# packages/device_tracker/luci.yaml if the router support returns.
# device_tracker/luci:
#   device_tracker:
#     - host: !secret luci_host
#       password: !secret luci_password
#       platform: luci
#       username: !secret luci_username
