Review the most recent session from (.copilot/sessions).

No entities are populating for the custom vesync integration. Use `ha core logs`, and `ha core restart` to diagnose and fix the issue. There should be at least 3 devices populated (one air purifier, and two humidifiers).
